── P0 -- real parity + upstream sync loop ──

── STATE ──
Suite 282/0/0. 85 tools, 80 CLI, 37 config sections, 19 GW, 10 prov, 65 libs.
v116. 33 real parity gaps (5 S0 + 5 S1 + 4 S2 + 6 S3 + 7 S4 NEW).
276 C commits ahead of upstream. 7583 upstream commits behind (fork 2517917de).
state.md ground truth. battleship-v27.md canonical gap list.

── LOOP ──
Read state → battleship → prestige → plan → overnight.
Pick next gap. Verify claim against BOTH C source AND Python source.
Implement. Build. Suite. Cross-check against Python behavior.
Doc sweep ALL walkways version-bumped. Barnacle hunt.
Vault resolved with file:line evidence. Remove from battleship.
Commit. Push. Repeat. No questions. No choices.

── DOC DISCIPLINE ──
After EVERY implementation: update ALL walkway files (state,
goal-mantra, prestige, overnight, plan, battleship) AND README
AND BANNER AND entry.md AND index.md AND testing.md AND vault.
Version-bump all in lockstep. Grep for old numbers across the
entire markdown tree after every batch.

── VAULT RULES ──
Resolved gaps → vault/achievements.md with file:line evidence.
Battleship carries ONLY active gaps — no checkmarks, no stale items.
Vault is history. Battleship is TODO.

── MEMORY RULES ──
Session state, PR numbers, commit SHAs → vault or walkway, not memory.
Memory: user preferences, env facts, durable patterns only.

── HONESTY ──
"0 gaps" was wrong. Don't claim parity without behavioral verification.
Suite pass != product works. Single-turn != multi-turn. Compiles != functions.
Test what the Python tests test. No cheating.
Upstream drift is real — 7583 unported commits. Audit before claiming parity.
