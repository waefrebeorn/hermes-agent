── P0 -- gap-closing loop ──

── STATE ──
Suite 282/0/0. 96 tools, 98 CLI, 19 GW, 10 prov, 65 libs.
v114. 0 gaps. ALL RESOLVED.
state.md ground truth. battleship-v24.md canonical gap list.

── LOOP ──
Read state → battleship → prestige → plan → overnight.
Pick next gap. Verify claim against C source first.
Implement. Build. Suite. Doc sweep ALL walkways.
Barnacle hunt stale numbers across every .md + README + BANNER.
Vault resolved gaps with file:line evidence. Remove from battleship.
Commit. Push. Repeat. No questions. No choices. Never stop.

── DOC DISCIPLINE ──
After EVERY implementation: update ALL walkway files (state,
goal-mantra, prestige, overnight, plan, battleship) AND README
AND BANNER AND entry.md AND index.md AND testing.md AND vault.
Version-bump all in lockstep. Grep for old numbers across the
entire markdown tree after every batch. Every file with multiple
totals = update ALL copies. Stale numbers compound silently.

── VAULT RULES ──
Resolved gaps → vault/achievements.md with file:line evidence.
Stale claims discovered during verification → vault with explanation.
Battleship carries ONLY active gaps — no checkmarks, no stale items.
Vault is history. Battleship is TODO. Keep them cleanly separated.

── MEMORY RULES ──
Save to MEMORY: user preferences, env facts, tool quirks, stable
conventions, project-level patterns. Durable facts that survive
across sessions.
Do NOT save to MEMORY: task progress, PR numbers, commit SHAs,
session outcomes, TODOs, version numbers, gap counts. These live
in vault (historical record) and walkway files (state).
session_search for cross-session recall. Skills for procedural
patterns. Memory at capacity → consolidate, don't expand.

── NO TIME ──
No dates. No pacing. No duration estimates. No human-week framing.
AI is time-irrelevant. This is agent work. Work continues until
done. The next session picks up where this one left off.

── BOUNDARIES ──
Skills hold REUSABLE patterns only. No project-specific paths,
file trees, version numbers, commit counts, or milestone percents.
Those belong in memory or mind-palace. Skills must survive a
different project starting fresh tomorrow.

── NEXT ──
Pick the next unclosed gap from the battleship. Verify the claim
against actual C source code before implementing — many claims
are stale. When in doubt, force one edit, build, commit. The
loop breaks on the first compilation. Report result, don't ask
permission. KEEP WORKING.

── CAVEMAN ──
This paste only. Walkway files in full prose. Resume caveman for
all other output.
