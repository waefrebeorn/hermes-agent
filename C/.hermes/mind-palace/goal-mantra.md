── STATE: battleship in C/.hermes/mind-palace/.
   Suite 282/0/0. 99 tools, 98 CLI, 19 GW, 10 providers, 66 libs.
   v98. 21 gaps (3 partial). D07 partial: spawn pause.
   Build: make -j$(nproc). Test: bash test_runner.sh.
   All metrics verified: state.md is ground truth.
   vault/achievements.md records every resolved gap.

── LOOP: read state.md → battleship → prestige → plan → overnight →
   pick first unclosed gap → implement → build → test → update ALL docs →
   search_files for stale numbers across all markdowns →
   vault with evidence → commit → repeat.
   No questions. No choices. Exhaust only: awaiting direction.

── PHASE ORDER: follow battleship phase order. Pick the next undone item
   from the highest priority phase. Verify claims against C source before
   implementing -- many are stale.

── DOC DISCIPLINE: after every implementation pass, update ALL walkway
   files (state, goal-mantra, prestige, overnight, plan) AND README
   AND banner AND battleship AND testing.md AND entry.md AND index.md
   AND ARCHITECTURE.md. Barnacle hunt stale numbers across ALL .md
   files after every patch batch. Version-bump all when numbers change.

── VAULT: resolved gaps → vault/achievements.md with file:line evidence.
   Stale claims → vault with explanation. Battleship carries only active
   gaps -- no ✅, no stale items. Vault is the historical record; battleship
   is the TODO list.

── MEMORY: save user preferences and durable env facts. Do NOT save task
   progress, PR numbers, commit SHAs, or session outcomes. session_search
   for cross-session recall. Procedural patterns as skills. Vault entries
   capture evidence; memory captures reusable knowledge.

── NO TIME: no dates, no pacing, no effort framing. Every gap is code.
   Work until done, pick next. Exhaust only: "awaiting direction."

── CAVEMAN: this paste only. Walkway files in full prose.

KEEP WORKING
